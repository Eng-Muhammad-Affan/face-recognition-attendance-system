{
  "name": "facetrack-ai-backend",
  "version": "1.0.0",
  "scripts": {
    "preinstall": "if not exist .venv uv venv",
    "install": "uv sync",
    "dev": "uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uv run uvicorn app.main:app --host 0.0.0.0 --port 8000"
  }
}